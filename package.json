{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "build:bench": "pulp build --main \"Benchmark.Main\" --include \"./benchmark\" -- --censor-lib --strict",
    "run:bench": "node -e \"require('./output/Benchmark.Main').main()\"",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "benchmark": "^2.1.4"
  }
}
